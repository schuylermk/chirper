<header>
  <div class="page-container">
    <div class="logo"></div>

    <nav>
      {{#link-to "home"}}Home{{/link-to}}

      {{#link-to "user"}}
        <img class="avatar" src="/images/avatar.jpg" />
        t4t5
      {{/link-to}}

      {{#if addNewChorp}}
        {{#new-chorp}}{{/new-chorp}}
        <button class="hide" {{action "hideForm"}}>Chiirrrop, NO!</button>
      {{else}}
        <button class="chirp" {{action "newChorp"}}>Chirrrop!</button>
      {{/if}}
    </nav>

  </div>
</header>

{{outlet}}
